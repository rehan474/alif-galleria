import {products} from '../data/site.js';import {WhatsApp} from './whatsapp.js';
function card(p,i){return `<article class="product-card reveal" style="--delay:${(i%3)*80}ms"><div class="product-image-wrap"><img src="${p.image}" alt="${p.title}" loading="lazy"><div class="image-number">${String(i+1).padStart(2,'0')}</div><div class="product-hover"><span>Enquire</span></div></div><div class="product-body"><span class="product-category">${p.category}</span><h3>${p.title}</h3>${WhatsApp({message:p.message,children:'Enquire on WhatsApp'})}</div></article>`}
export function Products(){return `<section id="products" class="section-pad products-section"><div class="container"><div class="section-head reveal"><div><span class="eyebrow">What we offer</span><h2>Products with purpose.</h2></div><p>Choose a category and start a direct WhatsApp enquiry with the product name already included.</p></div><div class="filter-bar reveal" data-product-filters>${['All','Doors','Windows','Panels','Fabrication'].map(f=>`<button class="${f==='All'?'active':''}" data-filter="${f}">${f}</button>`).join('')}</div><div class="products-grid" id="products-grid">${products.map(card).join('')}</div></div></section>`}
export function initProducts(){const root=document.querySelector('#products');if(!root)return;const buttons=root.querySelectorAll('[data-filter]');const grid=root.querySelector('#products-grid');buttons.forEach(btn=>btn.addEventListener('click',()=>{buttons.forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;grid.innerHTML=products.filter(p=>f==='All'||p.category===f).map(card).join('');window.initEffects?.(grid)}))}
